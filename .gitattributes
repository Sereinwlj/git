# 换行符策略
#
# 为什么需要这个文件：
# Git 在 Windows 上默认会把文本文件的换行符从 LF 转成 CRLF。对源码来说影响不大，
# 但对判题系统是要命的 —— 测试数据（.in / .out）一旦被转换，逐行比对时就可能因为
# 多出来的 \r 而误判成 WrongAnswer，而且这种错误在不同操作系统上表现还不一样。
#
# 所以策略是分开的：
#   源码和文档  -> 统一用 LF，保证跨平台一致
#   判题测试数据 -> 标记为 -text（不参与换行转换），原样按字节保存

# 所有文本文件，入库统一为 LF
* text=auto eol=lf

# 判题测试数据：保持原始字节，禁止任何换行符转换
problems/**/data/*.in   -text
problems/**/data/*.out  -text

# 二进制文件明确标记，避免被误判为文本
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.zip   binary
*.gz    binary
*.exe   binary
*.db    binary
